{
  "version": 2,
  "builds": [
    {
      "src": "view/*",
      "use": "@vercel/build",
      "config": {
        "buildCommand": "vercel build"
      }
    },
    {
      "src": "!(view/**)",
      "use": "@vercel/node"
    }
  ]
}
