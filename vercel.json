{
  "version": 2,
  "builds": [
    {
      "src": "view/*",
      "use": "@vercel/build",
      "config": {
        "buildCommand": "npm run build"
      }
    },
    {
      "src": "!(view/**)",
      "use": "@vercel/node"
    }
  ]
}
